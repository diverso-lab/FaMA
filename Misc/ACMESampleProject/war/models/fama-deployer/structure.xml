<?xml version="1.0" encoding="UTF-8"?>

<!--
	
	* Las propiedades de los tipos de nodos (mapping, label, staticContent...) son usadas LIBREMENTE por los
	  (ACME-) widget. Esto significa que aunque los widget por defecto usen, por ejemplo, la propiedad "mapping"
	  siguiendo los criterios que aquí se establecen, si se creara un nuevo widget de procesado, queda a elección
	  del autor usarlos como aquí se dice.
	  
	* Los tags de metainformación (palabras reservadas, con o sin parámetros, que empiezan por "%"),
	  sirven, en general, para imprimir información interna del árbol.

-->

<model name="Arbol" version="1" revision="0">
	<restrictionNodetypes>
		
		<!--
			mapping:
				true (por defecto) -> queremos que el atributo se procese.
				false -> queremos ignorarlo al procesar el árbol.
				
			label:
				true -> queremos que el valor del atributo se use en la interfaz para
						identificar al nodo.
				false (por defecto) -> se tratará como un atributo normal.
		-->
		<type name="FaMa-Feature-model" displayName="FaMa Feature model">
			<param name="Name" value="FaMa Feature model" label="true" mapping="false" control="Constant"></param>
			<!-- <param name="Densidad" value="1,10,100,1000,X" control="Enum"></param>  -->
		</type>
		
		<type name="Node">
			<param name="Name" value="Feature name" label="true" mapping="true" control="Constant"></param>
			<param name="Selected" value="false" label="true" mapping="true" control="Checkbox"></param>
			<!-- <param name="Densidad" value="1,10,100,1000,X" control="Enum"></param>  -->
		</type>
		
		
		
		
		
		<!--
			pre: Texto que se desea imprimir justo antes del nodo.
			pos: Texto que se desea imprimir justo después del nodo.
			
			%BREAK -> imprime un salto de linea.
		-->
		<!-- 
		<type name="Tronco" pre="pre%BREAK" pos="%BREAKpos" displayName=": %ATTB_PARAM[Nombre]">
			<param name="Nombre" value="miTronco %LOCATION_HREF %ATTB_PARAM[1]" label="true" mapping="false"></param>
			<param name="Longitud" value="1"></param>
		</type>
		
		<type name="Ramas">
			<param name="Nombre" value="misRamas - %ATTB_PARAM[2] - (%ATTB_PARAM[1])" label="true" mapping="false" control="Hidden"></param>
			<param name="volumen" value="50"></param>
			<param name="Densidad" value="-n/a-" control="Enum"></param>
		</type> -->
		
		<!--
			collapsed:
				true -> si queremos que el nodo aparezca por defecto colapsado.
				false (por defecto) -> queremos que aparezca inicialmente abierto.
		-->
		<!-- 
		<type name="Frutos" abstract="true" collapsed="true">
			<param name="Nombre" value="Nodo abstracto para Fruto" label="true" mapping="false"></param>
		</type>
		 -->
		<!--
			leaf:
				true -> si queremos que el nodo sea hoja.
				false (por defecto) -> si queremos que sea normal.
		-->
		<!-- 
		<type name="Hoja" leaf="true">
			<param name="Nombre" value="miHoja" label="true" mapping="false"></param>
			<param name="Familia" value="Peciolada"></param>
		</type>
		 -->
		<!--
			staticContent: Contenido estático que se desea incluir dentro del nodo. Muy útil para
						   los nodos hojas. Se puede incluir texto normal mezclado con metainformación.
			
			control: widget con el que se quiere mostrar el valor del atributo.
				Field (por defecto) -> campo de texto.
				Checkbox -> usado para booleanos.
				Constant -> el valor aparecería como texto no editable.
				
			%ATTB_PARAM[<nombre del atributo>] -> imprime el valor del nombre de atributo
												  que se le pasa como parámetro.
		-->
		<!-- 
		<type name="Fruto" leaf="true" staticContent="%ATTB_PARAM[Calidad]">
			<param name="Nombre" value="miFruto" label="true" mapping="false"></param>
			<param name="Calidad" value="Media" mapping="false"></param>
			<param name="Maduro" value="false" control="Checkbox"></param>
		</type>
		 -->
	</restrictionNodetypes>
</model>